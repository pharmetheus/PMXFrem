% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fremModelInfo.R
\name{fremModelInfo}
\alias{fremModelInfo}
\title{Derive the structural integer arguments of a FREM model}
\usage{
fremModelInfo(modFile, dfext, numNonFREMThetas = NULL, numSkipOm = NULL)
}
\arguments{
\item{modFile}{Path to the FREM model file. Must contain the PsN-style
\verb{;;;FREM CODE BEGIN COMPACT} / \verb{;;;FREM CODE END COMPACT} markers (models from
\code{createFREMmodel()}, \code{updateFREMmodel()} and \code{createMinimalFremModel()} all
carry them) and a final \verb{$OMEGA BLOCK(N)} record.}

\item{dfext}{A data frame with \verb{THETA*} / \verb{OMEGA*} / \verb{SIGMA*} columns - a
\code{getExt()} result or a \code{PMXForest::getSamples()} result - or a path to a
NONMEM \code{.ext} file.}

\item{numNonFREMThetas, numSkipOm}{Optional. If supplied they are returned as
given (an override), but a value that disagrees with the one derived from
the model triggers a warning. Leave \code{NULL} (default) to use the derived
value.}
}
\value{
A list with components \code{numNonFREMThetas}, \code{numFREMThetas},
\code{numParCov}, \code{numSkipOm}, \code{numSigmas}, \code{numTotEta}, \code{numTotThetas},
\code{covNames}, \code{orgCovNames} and \code{polyCatCovs}.
}
\description{
Works out \code{numNonFREMThetas}, \code{numSkipOm}, \code{numParCov}, \code{numFREMThetas} and
\code{numSigmas} from a FREM model file and its \code{.ext} (or a \code{getSamples()} result),
so the user does not have to supply them by hand to the FREM analysis
functions.
}
\details{
The derivation is:

\preformatted{
numFREMThetas    = length(getCovNames(modFile)$covNames)
numTotThetas     = number of THETA columns in `dfext`
numNonFREMThetas = numTotThetas - numFREMThetas
numTotEta        = solve k(k+1)/2 = number of OMEGA columns in `dfext`
blockN           = N in the final "$OMEGA BLOCK(N)" record of `modFile`
numParCov        = blockN - numFREMThetas
numSkipOm        = numTotEta - blockN
numSigmas        = number of SIGMA columns in `dfext`
}

\code{numSkipOm} is taken as \code{numTotEta - blockN} rather than by counting the simple
\verb{$OMEGA} records before the block; that is robust to a structural correlation
\verb{$OMEGA BLOCK(k)} sitting before the FREM block.
}
\examples{
modFile <- system.file("extdata/SimNeb/run31.mod", package = "PMXFrem")
extFile <- system.file("extdata/SimNeb/run31.ext", package = "PMXFrem")
fremModelInfo(modFile, extFile)

}
\seealso{
Other NONMEM Parsers: 
\code{\link{getCovNames}()},
\code{\link{getExt}()},
\code{\link{getFileNames}()},
\code{\link{getPhi}()}
}
\concept{NONMEM Parsers}
\concept{nonmem_parsers}
