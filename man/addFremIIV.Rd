% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addFremIIV.R
\name{addFremIIV}
\alias{addFremIIV}
\title{Add an IIV (ETA) to an established FREM model}
\usage{
addFremIIV(
  strFREMModel,
  parameter = NULL,
  omegaInit,
  link = c("exp", "add", "none"),
  numNonFREMThetas = NULL,
  numSkipOm = NULL,
  extFile = NULL,
  label = NULL,
  newModel = NULL,
  bWriteMod = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{strFREMModel}{Path to the FREM model file, or a character vector of its
lines. A path is required if \code{numNonFREMThetas} / \code{numSkipOm} are to be
derived (they are read from the model + its \code{.ext} via \code{\link[=fremModelInfo]{fremModelInfo()}}).}

\item{parameter}{Name of the \verb{$PK} variable that receives the new \code{ETA()}.
Required unless \code{link = "none"}. Must be assigned on exactly one \verb{$PK} line.}

\item{omegaInit}{Initial variance for the new \verb{$OMEGA} record. Required - no
default, since the value is a modelling choice (a small value such as
\code{0.01} or \code{0.04} is typical for a fresh IIV).}

\item{link}{One of \code{"exp"} (default), \code{"add"} or \code{"none"} - see Details.}

\item{numNonFREMThetas, numSkipOm}{Structural integers. If \code{NULL} (default)
they are derived from the model + \code{.ext}; supply them to override the
derivation (or when \code{strFREMModel} is a character vector).}

\item{extFile}{Path to the \code{.ext} used for the derivation. Defaults to the
model path with a \code{.ext} extension.}

\item{label}{Comment for the new \verb{$OMEGA} record. Defaults to
\code{"IIV on <parameter>"}.}

\item{newModel}{Output path when \code{strFREMModel} is a path and
\code{bWriteMod = TRUE}. Defaults to the input path with \verb{_iiv} inserted before
the extension.}

\item{bWriteMod}{Logical. Write the new model to \code{newModel}? Ignored when
\code{strFREMModel} is a character vector. Default \code{TRUE}.}

\item{quiet}{Logical. Suppress the progress message. Default \code{TRUE}.}
}
\value{
Invisibly, a list with
\item{model}{the new model as a character vector}
\item{etaIndex}{the index of the inserted \code{ETA()} (\code{numSkipOm + 1})}
\item{numSkipOm}{the updated skip count (old \code{+ 1})}
\item{numNonFREMThetas}{unchanged, passed through for convenience}
\item{file}{the path written, or \code{NULL}}
}
\description{
Inserts one new inter-individual-variability random effect into a FREM model,
in the "skip" region that precedes the FREM \verb{$OMEGA BLOCK(N)}. The new
\code{ETA()} takes index \code{numSkipOm + 1}; every \code{ETA()}, \code{MU_} and \code{COV}
reference at or after that index in \verb{$PK} / \verb{$ERROR} (including the
\verb{;;;FREM CODE} block) is shifted up by one so the model stays consistent, and
a matching simple \verb{$OMEGA} record is inserted immediately before the FREM
block. \code{numSkipOm} therefore increases by one; \code{numParCov}, the FREM
\code{BLOCK(N)} and the FREM covariate structure are untouched.

This is the primitive used by \code{\link[=addFremStructuralTheta]{addFremStructuralTheta()}}; call it directly
only when you want to add a random effect without a new structural \verb{$THETA}.
}
\details{
The existing \code{.ext} / \code{.phi} are \strong{not} migrated - a model with a new random
effect must be re-estimated. Only the control stream is rewritten.

\code{link} controls how the new \code{ETA()} enters the target \verb{$PK} line:
\describe{
\item{\code{"exp"}}{\verb{<param> = (<rhs>) * EXP(ETA(k))} - log-normal IIV, the
FREM convention.}
\item{\code{"add"}}{\verb{<param> = <rhs> + ETA(k)} - additive IIV.}
\item{\code{"none"}}{the \verb{$PK} line is left alone; the renumber pass and the new
\verb{$OMEGA} record are still applied. Use this when the caller writes the
\code{ETA()} reference itself (e.g. \code{\link[=addFremStructuralTheta]{addFremStructuralTheta()}} with
\code{muReference = TRUE}).}
}
}
\seealso{
\code{\link[=addFremStructuralTheta]{addFremStructuralTheta()}}, \code{\link[=updateFREMmodel]{updateFREMmodel()}}

Other FREM model management: 
\code{\link{addFremStructuralTheta}()},
\code{\link{createFREMmodel}()},
\code{\link{updateFREMmodel}()}
}
\concept{FREM model management}
\concept{frem_model_management}
