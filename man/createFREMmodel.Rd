% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createFREMmodel.R
\name{createFREMmodel}
\alias{createFREMmodel}
\title{Create a Complete FREM Model}
\usage{
createFREMmodel(
  runno = NULL,
  modName = NULL,
  modDevDir = NULL,
  ffemDataFile,
  covariates,
  numNonFREMThetas,
  outputDir = NULL,
  fremModName = "frem_model",
  keepMinimalModel = FALSE,
  catCovs = NULL,
  logtCovs = NULL,
  IDvar = "ID",
  missVal = -99,
  fixTheta = TRUE,
  roundMeanTo = 1,
  useMuModeling = TRUE,
  numSkipOm = 0,
  cstrKeepCols = c("ID", "TIME", "AMT", "II", "EVID", "SS", "RATE", "DV"),
  quiet = FALSE,
  bRecodeDichotomous = FALSE,
  allowNon01 = FALSE,
  keepDoseOnlySubjects = FALSE,
  ...
)
}
\arguments{
\item{runno}{Standard PMX run number.}

\item{modName}{Standard PMX model name.}

\item{modDevDir}{Standard PMX model development directory.}

\item{ffemDataFile}{Character. Path to the Full Fixed Effects Model (FFEM) dataset.}

\item{covariates}{Character vector. The full list of covariates to include in the FREM model.
The first element is processed during Phase 1 (bootstrapping), and the remainder during Phase 2.}

\item{numNonFREMThetas}{Numeric. The number of THETAs in the base model that are not part of the FREM structure.}

\item{outputDir}{Character. Directory where the final model and data files will be saved. Defaults to \code{modDevDir} or current working directory.}

\item{fremModName}{Character. The base name for the generated final model and data files (e.g., \code{"frem_model"}).}

\item{keepMinimalModel}{Logical. Should the intermediate minimal model files (Phase 1 output) be preserved? Defaults to \code{FALSE}.}

\item{catCovs}{Character vector. Names of the covariates in \code{covariates} that should be treated as categorical.}

\item{logtCovs}{Character vector. Names of continuous covariates in \code{covariates} that should be log-transformed (natural log).}

\item{IDvar}{Character. The name of the subject identifier column in the dataset. Defaults to \code{"ID"}.}

\item{missVal}{Numeric. The value representing missing data in the covariates. Defaults to \code{-99}.}

\item{fixTheta}{Logical. Should the initial THETA estimates for the covariates be fixed? Defaults to \code{TRUE}.}

\item{roundMeanTo}{Numeric. The number of decimal places to round the calculated baseline covariate means. Defaults to \code{1}.}

\item{useMuModeling}{Logical. Should MU-referencing be utilized when generating the NONMEM $PK and $ERROR blocks? Defaults to \code{TRUE}.}

\item{numSkipOm}{Numeric. Number of initial OMEGA parameters to skip when appending new FREM parameters. Defaults to \code{0}.}

\item{cstrKeepCols}{Character vector. Columns to strictly retain in the generated FREM dataset. Note: \code{"FREMTYPE"} is managed internally and injected between phases.}

\item{quiet}{Logical. Should the function execute silently without console messages? Defaults to \code{FALSE}.}

\item{bRecodeDichotomous}{Logical. Should dichotomous covariates be automatically
recoded to 0/1? Defaults to FALSE. If FALSE, inputs must be strictly 0/1.}

\item{allowNon01}{Logical. If TRUE, allows non-0/1 dichotomous covariates (like 1/2)
to pass through untouched for PsN compatibility. Defaults to FALSE.}

\item{keepDoseOnlySubjects}{Logical. If \code{FALSE} (default), subjects without any valid PK observations (e.g.,
only dosing records) are completely excluded from the generated dataset. If \code{TRUE},
these subjects are retained and their covariates are included as observations.}

\item{...}{Additional arguments strictly passed to the Phase 1 bootstrapper (\code{createFREMmodel_phase1}).}
}
\value{
An invisible list containing the file paths to the generated assets:
\itemize{
\item \code{model}: Path to the final generated \code{.mod} file.
\item \code{data}: Path to the final generated \code{.csv} data file.
}
}
\description{
The master orchestrator for generating a Full Random Effects Model (FREM) in NONMEM.
This wrapper manages a two-phase pipeline: it bootstraps a minimal model with the first
covariate (handling any necessary categorical Y-1 expansions), iteratively injects
the remaining covariates, safely manages \code{FREMTYPE} sequencing between phases, and
cleans up all intermediate files.
}
\examples{

# Setup paths using package extdata
modDevDir    <- system.file("extdata", "SimNeb", package = "PMXFrem")
modName      <- "run30"
ffemDataFile <- system.file("extdata", "SimNeb/DAT-2-MI-PMX-2-onlyTYPE2-new.csv", package = "PMXFrem")


td <- tempfile(pattern = "frem_example_")
dir.create(td)

# Define covariates and keep columns
covariates <- c("AGE","WT","SEX","RACEL")
catCovs    <- c("RACEL","SEX")
keep_cols  <- c("ID", "TIME", "AMT", "EVID", "RATE", "FOOD", "DV")

# Generate the full FREM model
generated_files <- createFREMmodel(
  modName            = modName,
  modDevDir          = modDevDir,
  ffemDataFile       = ffemDataFile,
  covariates         = covariates,
  logtCovs           = "WT",
  catCovs            = catCovs,         
  outputDir          = td,
  fremModName       = "frem_final",
  numNonFREMThetas   = 7,
  numSkipOm          = 2,
  cstrKeepCols       = keep_cols,
  bRecodeDichotomous = TRUE,
  quiet              = TRUE
)

# The generated files are available in the temporary directory
print(generated_files$model)
print(generated_files$data)

#Clean up the temporary directory
unlink(td, recursive = TRUE)

}
\seealso{
Other FREM model management: 
\code{\link{updateFREMmodel}()}
}
\concept{FREM model management}
\concept{frem_model_management}
