% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createFREMmodel_phase1.R
\name{createFREMmodel_phase1}
\alias{createFREMmodel_phase1}
\title{Bootstrap the Minimal FREM Model (Phase 1)}
\usage{
createFREMmodel_phase1(
  runno = NULL,
  modName = NULL,
  modDevDir = NULL,
  ffemDataFile,
  covariates,
  outputDir = NULL,
  minModName = "minimal_model",
  keepMinimalModel = FALSE,
  cstrKeepCols = c("ID", "TIME", "AMT", "II", "EVID", "SS", "RATE"),
  numSkipOm = 0,
  IDvar = "ID",
  missVal = -99,
  fixTheta = TRUE,
  roundMeanTo = 1,
  catCovs = NULL,
  logtCovs = NULL,
  useMuModeling = TRUE,
  bRecodeDichotomous = FALSE,
  allowNon01 = FALSE,
  keepDoseOnlySubjects = FALSE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{runno}{Standard PMX run number.}

\item{modName}{Standard PMX model name.}

\item{modDevDir}{Standard PMX model development directory.}

\item{ffemDataFile}{Character. Path to the base FFEM dataset.}

\item{covariates}{Character vector. The full list of covariates to be added to the FREM model. Phase 1 strictly processes the first element.}

\item{outputDir}{Character. Directory where the minimal files will be saved. Defaults to \code{modDevDir} or current working directory.}

\item{minModName}{Character. The base name for the generated minimal files. Defaults to \code{"minimal_model"}.}

\item{keepMinimalModel}{Logical. Should the minimal files be retained after the full pipeline completes? Passed back to the master wrapper. Defaults to \code{FALSE}.}

\item{cstrKeepCols}{Character vector. Columns to strictly retain in the generated FREM dataset.}

\item{numSkipOm}{Numeric. Number of initial OMEGA parameters to skip when appending new FREM parameters. Defaults to \code{0}.}

\item{IDvar}{Character. The name of the subject identifier column. Defaults to \code{"ID"}.}

\item{missVal}{Numeric. The value representing missing data in the covariates. Defaults to \code{-99}.}

\item{fixTheta}{Logical. Should the initial THETA estimates for the covariates be fixed? Defaults to \code{TRUE}.}

\item{roundMeanTo}{Numeric. The number of decimal places to round the calculated baseline covariate means. Defaults to \code{1}.}

\item{catCovs}{Character vector. Names of the covariates that should be treated as categorical.}

\item{logtCovs}{Character vector. Names of continuous covariates that should be log-transformed.}

\item{useMuModeling}{Logical. Should MU-referencing be utilized when generating the NONMEM $PK and $ERROR blocks? Defaults to \code{TRUE}.}

\item{bRecodeDichotomous}{Logical. Should dichotomous covariates be automatically
recoded to 0/1? Defaults to FALSE. If FALSE, inputs must be strictly 0/1.}

\item{allowNon01}{Logical. If TRUE, allows non-0/1 dichotomous covariates (like 1/2)
to pass through untouched for PsN compatibility. Defaults to FALSE.}

\item{keepDoseOnlySubjects}{Logical. If \code{FALSE} (default), subjects without any valid PK observations (e.g.,
only dosing records) are completely excluded from the generated dataset. If \code{TRUE},
these subjects are retained and their covariates are included as observations.}

\item{quiet}{Logical. Should the function execute silently without console messages? Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed down to internal helper functions (e.g., \code{createFREMData}, \code{prepareAndValidateData}).}
}
\value{
An invisible list containing:
\itemize{
\item \code{minimalModelFile}: Path to the generated minimal \code{.mod} file.
\item \code{minimalDataFile}: Path to the generated minimal \code{.csv} data file.
\item \code{mockExtFile}: Path to the generated mock \code{.ext} file.
\item \code{keepMinimalModel}: Boolean flag passed back to the master wrapper for cleanup logic.
\item \code{validatedData}: The filtered and validated FFEM dataset as a \code{data.frame}.
}
}
\description{
This is the internal bootstrapper function for the FREM generation pipeline. It reads
the base NONMEM model and the Full Fixed Effects Model (FFEM) dataset, processes strictly
the first covariate from the provided list, and generates a minimal FREM data file,
\code{.mod} file, and a mock \code{.ext} file. It inherently handles dynamic Y-1 categorical
expansions (e.g., splitting a 3-level categorical variable into two dummy variables)
and establishes the initial \code{FREMTYPE} sequencing.
}
\seealso{
\code{\link{createFREMmodel}}, \code{\link{updateFREMmodel}}

Other FREM model management Internal: 
\code{\link{createMinimalFremModel}()},
\code{\link{generateFremModel}()},
\code{\link{initializeModelParameters}()},
\code{\link{removeFremCovariates}()}
}
\concept{FREM model management Internal}
\concept{frem_model_management}
\keyword{internal}
