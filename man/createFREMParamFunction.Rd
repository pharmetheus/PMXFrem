% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createFREMParamFunction.R
\name{createFREMParamFunction}
\alias{createFREMParamFunction}
\title{Generate a FREM parameter function from the FREM model's \verb{$PK}}
\usage{
createFREMParamFunction(
  fremModel = NULL,
  parameters,
  numSkipOm = NULL,
  numNonFREMThetas = NULL,
  runno = NULL,
  modName = NULL,
  modDevDir = NULL,
  dfext = NULL,
  extFile = NULL,
  numParCov = NULL,
  covRef = NULL,
  functionName = "paramFunction",
  file = NULL,
  missVal = -99,
  quiet = FALSE,
  secondary = NULL
)
}
\arguments{
\item{fremModel}{Path to the FREM NONMEM control stream (\code{.mod} / \code{.ctl}).
Optional if \code{runno} / \code{modName} (+ \code{modDevDir}) are given.}

\item{parameters}{Character vector of \verb{$PK} variables the generated function
should return. List the FREM covariate parameters \strong{in the order of the FREM
parameter block} (that order is what \code{covthetas} / \code{etas} index); other
\verb{$PK} quantities may be included and are returned as-is.}

\item{numSkipOm, numNonFREMThetas}{The number of skipped omegas and of
structural thetas. \code{NULL} (default) derives them from the FREM model and the
ext via \code{\link[=fremModelInfo]{fremModelInfo()}}; a supplied value that disagrees with the derived
one warns and is kept.}

\item{runno}{The run number. Must be provided if \code{modName} is NULL. Will be
used to create NONMEM model and results file names  with the structure
run#.mod and run#. NONMEM ext, phi and cov files will be assumed to be
named run# followed by the appropriate suffix (where # is \code{runno}).}

\item{modName}{The model name. Will be used together with \code{modExt}, \code{lstExt},
ext, phi and cov to form the NONMEM file names, for example
\code{modName}.mod for the model file.}

\item{modDevDir}{The directory where the files reside. Default is the current
directory, i.e. ".".}

\item{dfext, extFile}{A \code{getExt()} data frame / an \code{.ext} path, used only to
derive \code{numSkipOm} / \code{numNonFREMThetas}. Defaults to the ext located from
\code{runno} / \code{modName} / \code{modDevDir}, else the \code{.ext} beside \code{fremModel}.}

\item{numParCov}{Optional cross-check: should equal the number of
\code{parameters} that carry an \code{ETA()} in \verb{$PK}. A mismatch warns.}

\item{covRef}{Optional named list of structural-covariate reference values,
e.g. \code{list(WT = 75)}, forwarded to \code{\link[PMXForest:nmParsePK]{PMXForest::nmParsePK()}}.}

\item{functionName}{Name for the generated function. Default \code{"paramFunction"}.}

\item{file}{Optional path to write the generated source to. It is returned
either way.}

\item{missVal}{The value marking an inactive covariate. Default -99.}

\item{quiet}{If \code{FALSE} (default), reports what was found.}

\item{secondary}{Optional named list of secondary parameters to append to the
generated function's return list. Each entry's value is a single string (R
code whose last value is the result, \code{list(AUC = "dfrow$DOSE / CL")}, or
the path to an \code{.R} file of arbitrary code such as an \code{mrgsolve}
simulation), or a list \verb{list(source = <string>, dose = 100, tau = 12, ...)}
carrying that \code{source} plus named atomic constants bound ahead of it. The
code is spliced in inside a \code{local()} block and sees \code{basethetas},
\code{covthetas}, \code{dfrow} (also as \code{df}), \code{etas}, \code{...}, any constants passed
alongside \code{source}, and every structural parameter by name; covariate
columns are \code{dfrow$NAME}. Handled by \code{\link[PMXForest:nmResolveSecondary]{PMXForest::nmResolveSecondary()}}; see
\code{\link[PMXForest:createParamFunction]{PMXForest::createParamFunction()}}.}
}
\value{
A list:
\itemize{
\item \code{code} - the generated R source, class \code{"pmxFREMParamFunction"}.
\item \code{functionListName} - \code{parameters}, with any \code{secondary} names
appended (so \code{getForestDFFREM()} / \code{getExplainedVar()} pick them up).
\item \code{primaryNames} - \code{parameters} alone.
\item \code{secondaryNames} - the \code{secondary} names (\code{character(0)} when none).
\code{\link[=verifyFREMParamFunction]{verifyFREMParamFunction()}} skips these.
\item \code{fremParameters} - the subset of \code{parameters} that got the covariate
splice, in order.
\item \code{fremEtaScale} - named character over \code{fremParameters}: \code{"exp"} when
the \verb{$PK} line is \verb{C * exp(<linear in ETA>)} (log-normal), \code{"other"}
otherwise. \code{\link[=verifyFREMParamFunction]{verifyFREMParamFunction()}} uses this to decide whether the
\code{exp()} splice checks apply.
\item \code{noBaseThetas} - \code{numNonFREMThetas} (the length of \code{basethetas}).
\item \code{covRef} - the structural-covariate reference used, with its source.
\item \code{numParCov} - the number of \code{fremParameters}.
\item \code{numSkipOm}, \code{numNonFREMThetas}, \code{fremModel}, \code{missVal} - as
supplied or derived.
}
}
\description{
Translates the \verb{$PK} block of the \strong{FREM} model into R source for
a parameter function of the shape \code{getForestDFFREM()} and \code{getExplainedVar()}
expect, \verb{function(basethetas, covthetas, dfrow, etas, ...)}.

The structural \verb{$PK} is transliterated (structural, non-FREM covariates such
as allometric weight kept). A parameter in \code{parameters} that carries IIV in
\verb{$PK} (exactly one \code{ETA()} in its assignment) is a \strong{FREM covariate
parameter}: its \code{ETA()} reference is replaced \strong{in place} - whatever
encloses it, \code{exp(mu + ETA)}, \code{TV * exp(ETA)}, \code{TV + ETA}, ... - by
\code{covthetas[k] + etas[numSkipOm + k]}, because the FREM covariate coefficient
is additive on the same (eta) scale. A parameter with no \code{ETA()} is returned
as \verb{$PK} computes it (no covariate effect - not an error). Every other
\code{ETA(n)} is set to 0.

The emitted body is \strong{pruned to the statements the requested \code{parameters}
actually depend on}, so the FREM covariate block (\code{MU_j = THETA(...)} /
\code{COVj = MU_j + ETA(j)}) is dropped and \code{basethetas} is only the structural
thetas - the first \code{numNonFREMThetas}, exactly what \code{getForestDFFREM()} /
\code{getExplainedVar()} pass in.

The result is \strong{source text for you to read, check and edit} - nothing is
evaluated. One generated function serves both uses: call it with \code{etas = 0}
for a \code{getForestDFFREM()} forest plot, and with non-zero \code{etas} for a
\code{getExplainedVar()} explained-variability plot.
}
\details{
\strong{Why the FREM model, not the base model.} \code{numSkipOm} and the structural
\verb{$PK} / theta numbering are read from the model you are actually analysing.
If the FREM model was edited after it was built (extra \verb{$THETA}s or \verb{$OMEGA}s
added), the base model no longer describes it; the FREM model always does.

\strong{What \code{covthetas} and \code{etas} are.} \code{covthetas[k]} is the FFEM-projected
covariate coefficient for the k-th FREM covariate parameter (the k-th entry
of \code{parameters} that carries an \code{ETA()}), as computed by \code{\link[=calcFFEM]{calcFFEM()}} and
passed in by \code{getForestDFFREM()} / \code{getExplainedVar()}. \code{etas} carries the
random effects with the \code{numSkipOm} leading non-FREM omegas in front, so the
k-th parameter's structural eta is \code{etas[numSkipOm + k]}; the generated
function indexes it with a small \code{.eta()} helper that returns 0 when \code{etas}
is shorter (the explained-variability reference calls pass a long zero
vector).

\strong{Structural covariate references.} A covariate named in \verb{$INPUT} and used
but not assigned in \verb{$PK} needs a value for the rows where it is inactive
(\code{missVal}). The reference is taken from the control stream by the same
rules as \code{\link[PMXForest:createParamFunction]{PMXForest::createParamFunction()}} and hoisted into a preamble;
override or supply through \code{covRef}.

\strong{Which parameters get the covariate splice.} Exactly one \code{ETA()} -> the
parameter is spliced. None -> returned as-is (no covariate effect). More
than one -> returned as-is with a warning (which eta is "the" structural one
is ambiguous). \code{covthetas} and \code{etas[numSkipOm + ...]} are indexed by
position \strong{among the spliced parameters}, in \code{parameters} order. If the eta
index found for a spliced parameter is not \code{numSkipOm + k}, its position is
used anyway and a warning is issued.
}
\examples{
fremModel <- system.file("extdata/SimNeb/run31.mod", package = "PMXFrem")

# numSkipOm / numNonFREMThetas are derived from the FREM model and its ext.
# The ext is found next to the model automatically, so `extFile` is not
# needed here - pass `extFile` (or `dfext`) only when the .ext is elsewhere
# and cannot be reached through runno / modName / modDevDir.
out <- createFREMParamFunction(fremModel, parameters = c("CL", "V", "MAT"))
cat(out$code, sep = "\n")

# Same result, located by run number instead of a path:
out2 <- createFREMParamFunction(
  runno      = 31,
  modDevDir  = system.file("extdata/SimNeb/", package = "PMXFrem"),
  parameters = c("CL", "V", "MAT"))
stopifnot(identical(out$code, out2$code))

}
\seealso{
\code{\link[=verifyFREMParamFunction]{verifyFREMParamFunction()}}, \code{\link[PMXForest:nmParsePK]{PMXForest::nmParsePK()}},
\code{\link[=fremModelInfo]{fremModelInfo()}}, \code{\link[=getForestDFFREM]{getForestDFFREM()}}, \code{\link[=getExplainedVar]{getExplainedVar()}}.

Other Diagnostics & Plotting: 
\code{\link{calcEtas}()},
\code{\link{calcFremShrinkage}()},
\code{\link{fremParameterTable}()},
\code{\link{getExplainedVar}()},
\code{\link{getForestDFFREM}()},
\code{\link{plotCovDist}()},
\code{\link{plotEtasCov}()},
\code{\link{plotExplainedVar}()},
\code{\link{traceplot}()},
\code{\link{verifyFREMParamFunction}()}
}
\concept{Diagnostics & Plotting}
\concept{diagnostics}
