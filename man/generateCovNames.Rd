% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateCovNames.R
\name{generateCovNames}
\alias{generateCovNames}
\title{Generate and Format Covariate Labels for Forest Plots}
\usage{
generateCovNames(
  dfCovs,
  dfres = NULL,
  unit_map = NULL,
  label_map = NULL,
  print_template = FALSE
)
}
\arguments{
\item{dfCovs}{A data frame containing covariate scenarios (must have a \code{COVARIATEGROUPS} column).}

\item{dfres}{An optional results data frame generated by \code{\link{getForestDFFREM}}
or \code{\link{getExplainedVar}}. If provided, the function returns this data frame
with the \code{COVNAME} column updated and safely factored.}

\item{unit_map}{A named character vector for appending units (e.g., \code{c(WT = "kg")}).}

\item{label_map}{A named character vector for exact replacements (e.g., \code{c("SEX=1" = "Male")}).}

\item{print_template}{Logical. If \code{TRUE}, prints an R script template to the console and exits.}
}
\value{
If \code{dfres} is \code{NULL}, returns a character vector of formatted covariate names.
If \code{dfres} is provided, returns the updated \code{dfres} data frame.
}
\description{
Dynamically constructs covariate names from a forest plot covariate data frame.
If \code{dfres} is provided, it safely maps these generated labels directly onto the
results data frame using the \code{COVNUM} relational key, returning the updated data frame.

It features an interactive two-step workflow: run with \code{print_template = TRUE}
to generate a boilerplate mapping script in the console, then fill it out and
pass the maps back in to append units or replace categorical labels.
}
\examples{
# 1. Create a synthetic forest plot covariate dataframe
dfCovs <- data.frame(
  COVARIATEGROUPS = c("WT", "WT", "SEX", "SEX"),
  WT = c(70, 90, -99, -99),
  SEX = c(-99, -99, 1, 2)
)

# 2. Create a synthetic results dataframe (mimicking getForestDFFREM output)
dfres <- data.frame(
  COVNUM = c(1, 2, 3, 4),
  PARAMETER = rep("CL", 4),
  VALUE = c(1.1, 1.3, 0.9, 1.0)
)

# 3. Define Translation Maps
unit_map <- c(WT = "kg")
label_map <- c("SEX=1" = "Male", "SEX=2" = "Female")

# --- Example A: Extract Character Vector ---
# Returns standard formatted strings
generateCovNames(dfCovs, unit_map = unit_map, label_map = label_map)

# --- Example B: Relational Data Frame Mapping ---
# Safely mutates the data frame, adding the locked COVNAME factor
dfres_updated <- generateCovNames(
  dfCovs, 
  dfres = dfres, 
  unit_map = unit_map, 
  label_map = label_map
)

print(dfres_updated)
}
\seealso{
Other Diagnostics & Plotting Internal: 
\code{\link{calcParameterEsts}()}
}
\concept{Diagnostics & Plotting Internal}
\concept{diagnostics}
\keyword{internal}
