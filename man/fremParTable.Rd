% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fremParTable.R
\name{fremParTable}
\alias{fremParTable}
\title{fremParTable}
\usage{
fremParTable(
  runno,
  modDevDir,
  thetaNum,
  omegaNum,
  sigmaNum,
  numNonFREMThetas,
  numSkipOm,
  extFile,
  model.name = NULL,
  ignore.list = NULL,
  parnames = NULL,
  unitnames = NULL,
  includeRSE = FALSE,
  rseFile = NULL,
  n = 175,
  returnParTable = FALSE,
  parTable = NULL
)
}
\arguments{
\item{runno}{The run number of the FREM model.}

\item{modDevDir}{The path to the directory where the NONMEM run files are.}

\item{thetaNum}{The numbers of the thetas that should appear in the parameter table.}

\item{omegaNum}{The numbers of the omegas that should appear in the parameter table.}

\item{sigmaNum}{The numbers of the sogmas that should appear in the parameter table.}

\item{numNonFREMThetas}{Number of thetas in the FREM model that are not FREM
covariates. These need to come before the FREM covariate thetas in the FREM
model file.}

\item{numSkipOm}{Number of diagonal omegas that should not be part of the
FREM calculations. Such omegas has to come before the large FREM omega
block in the FREM model file.}

\item{extFile}{The path to the ext file of the FREM model.}

\item{model.name}{The name the model should have in the parameter table. (see model.name in PhRame::NM.parameter.table)}

\item{ignore.list}{A list of parameters that should not appear in the parameter table. (see ignore in PhRame::NM.parameter.table)}

\item{parnames}{The latex names of the parameters as they should appear in the table. (see print.names PhRame::NM.parameter.table)}

\item{unitnames}{The latex units as the should appear in the parameter table. (see unit.names PhRame::NM.parameter.table).}

\item{includeRSE}{Should RSEs be included in the parameter table (TRUE/FALSE).(The reverse of no.rse in PhRame::NM.parameter.table.}

\item{rseFile}{The path to a PsN bootstrap raw_results file or the .cov file from the FREM run. The former is u}

\item{n}{The number of samples from the variance-covariance matrix derived from the .cov or bootstrap raw_results file to base the
estimate of standard error on.}

\item{returnParTable}{Should the function return the data.frame with parameter estimate information instead of printing the latex code for the
table.}

\item{parTable}{A data.frame to be used as input to PhRame::NM.parameter.table.latex.}
}
\value{
Either printed latex code for the parameter table or a data frame suitable for PhRame::NM.Parameter.table.
}
\description{
Create a parameter table for frem models
}
\details{
The function uses the NM.Parameter.table and NM.Parameter.table.latex to parse the NONMEM run information and to print the latex code for the table.
The typical use is to take the output from a FREM model, which contains parameter estimates for all the covariates and the variances and covariances of the OMEGA BLOCK of
parameter etas and covariate etas, and reduce it down to the THETA estimates of the parameters in the base model and the diagonal omegas related to these. The omegas can either
be part of the FREM model specification or not. The omegas reported in the parameter table are either, in case the omega is not part of the FREM model specificaiton,
the omegas as reported in the NONMEM output or, in the case the omega is part of the NONMEM specification, adjusted for the covariates that are part of the model ("omega prim").

The RSEs are derived numerically by sampling from the variance-covariance matrix that is either as specified in the .cov file from NONMEM or the values in a PsN bootstrap raw_results file. The reason for
the numerical approach is that the SEs reported by NONMEM is for the omegas in the model and not for the omega prims.
}
\examples{
\dontrun{

parnames <- c("OFV","Condition number","\\\\glstext{CL}", "\\\\glstext{V}",
              "\\\\glstext{MAT}","\\\\glstext{MATFR}", "\\\\Food on \\\\glstext{FREL}","\\\\Food on \\\\glstext{MAT}",
              "\\\\glstext{IIV} \\\\glstext{RUV}", "\\\\glstext{IIV} \\\\glstext{FREL}", "\\\\glstext{IIV} \\\\glstext{CL}",
              "\\\\glstext{IIV} \\\\glstext{V}", "\\\\glstext{IIV} \\\\glstext{MAT}","\\\\glstext{RUV}")

unitnames  <- c("","","(L/h)","(L)","(h)", "","","","(CV)","(CV)","(CV)","(CV)","(CV)","(CV)")

thetaNum <- 4:9              # Thetas to appear in parameter table
omegaNum <- c(1,3:6)         # Omegas to appear in the parameter table
sigmaNum <- 1                # Sigmas to appear in the parameter table

myParTableFinal <- fremParTable(finalRun,
                                modDevDir,
                                thetaNum         = thetaNum,
                                omegaNum         = omegaNum,
                                sigmaNum         = sigmaNum,
                                numNonFREMThetas = EPtwoInfo$numNonFREMThetas,
                                numSkipOm        = EPtwoInfo$numSkipOm,
                                extFile          = extFile,
                                model.name       = "Final model",
                                ignore.list      = ("Condition number"),
                                parnames         = parnames2,
                                unitnames        = unitnames2,
                                includeRSE       = FALSE,
                                rseFile          = bsFile,
                                returnParTable   = TRUE
)
}
}
