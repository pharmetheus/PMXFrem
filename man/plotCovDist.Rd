% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCovDist.R
\name{plotCovDist}
\alias{plotCovDist}
\title{Plot the distribution of estimated FREM covariates by missingness}
\usage{
plotCovDist(
  df,
  covNames,
  plotType = c("histogram", "density"),
  xlab = "Estimated Covariate Value",
  ylab = "Count",
  fillNonMissing = "#005B65",
  fillMissing = "#E1A178",
  colorOutline = "white",
  alpha = 1,
  bins = 30,
  scales = "free_x",
  nrow = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{df}{A data frame generated by \code{\link{calcEtas}} (run with
\code{appendMissingFlags = TRUE}).}

\item{covNames}{A character vector specifying one or more covariates to plot
(e.g., \code{c("WT", "AGE")}).}

\item{plotType}{Character string specifying the geometry. Either \code{"histogram"}
(default) or \code{"density"}. Note that density plots are scaled to show counts
on the y-axis to match histograms.}

\item{xlab}{Character string for the x-axis title. Defaults to
\code{"Estimated Covariate Value"}.}

\item{ylab}{Character string for the y-axis title. Defaults to \code{"Count"}.}

\item{fillNonMissing}{Character string (hex code or color name) for the fill
of non-missing data. Defaults to \code{"#005B65"} (dark teal).}

\item{fillMissing}{Character string (hex code or color name) for the fill
of missing data. Defaults to \code{"#E1A178"} (peach).}

\item{colorOutline}{Character string for the outline of the geoms. Defaults
to \code{"white"}.}

\item{alpha}{Numeric. The transparency of the fills. Defaults to \code{0.6}.}

\item{bins}{Integer. Number of bins to use if \code{plotType = "histogram"}.
Defaults to \code{30}.}

\item{scales}{Character string to control axis scaling across facets. Options are
\code{"fixed"}, \code{"free"}, \code{"free_x"} (default), or \code{"free_y"}.}

\item{nrow}{Integer. Number of rows for the facet grid layout. Defaults to \code{NULL}.}

\item{ncol}{Integer. Number of columns for the facet grid layout. Defaults to \code{NULL}.}

\item{...}{Additional arguments passed directly to \code{geom_histogram()} or
\code{geom_density()} (e.g., \code{linewidth}, \code{bw}).}
}
\value{
A \code{ggplot} object.
}
\description{
Generates diagnostic distribution plots (histograms or density plots) of the
estimated covariates from a FREM model. The distributions are grouped and colored
by whether the covariate was missing in the original clinical dataset. This helps
evaluate if the model's empirical Bayes estimates for missing covariates map
consistently to the observed cohort's marginal distribution.

Note: In FREM, all estimated covariates are continuous. Therefore, covariates
that were originally binary/categorical will be plotted on a continuous scale.
}
\examples{
\dontrun{
library(PMXFrem)
library(ggplot2)

# 1. Prepare data and base model directories
model_dir <- system.file("extdata/SimNeb/", package = "PMXFrem")
data_path <- system.file("extdata/SimNeb/DAT-2-MI-PMX-2-onlyTYPE2-new.csv", package = "PMXFrem")
my_data <- read.csv(data_path)
my_data <- my_data[my_data$BLQ != 1, ]

# 2. Generate the individual ETAs and missingness flags
ind_params <- calcEtas(
  modName            = "run31",
  modDevDir          = model_dir,
  numNonFREMThetas   = 7,
  numSkipOm          = 2,
  dataFile           = my_data,
  parNames           = c("CL", "V", "MAT"),
  appendMissingFlags = TRUE
)

# 3. Plot a single covariate distribution (Histogram)
p1 <- plotCovDist(ind_params, covNames = "WT")
print(p1)

# 4. Plot multiple covariates simultaneously (Density curves of counts)
# Forcing a 2-column layout
p2 <- plotCovDist(
  df = ind_params, 
  covNames = c("WT", "AGE", "GENO2","AST"), 
  scales = "free",
  ncol = 2
)
print(p2)
}

}
\seealso{
Other Diagnostics & Plotting: 
\code{\link{calcEtas}()},
\code{\link{fremParameterTable}()},
\code{\link{getExplainedVar}()},
\code{\link{getForestDFFREM}()},
\code{\link{plotEtasCov}()},
\code{\link{plotExplainedVar}()},
\code{\link{traceplot}()}
}
\concept{Diagnostics & Plotting}
\concept{diagnostics}
