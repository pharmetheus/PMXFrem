% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind_estimates.R
\name{ind_estimates}
\alias{ind_estimates}
\title{ind_estimates}
\usage{
ind_estimates(cData, cTime, theta, om, sig, start_eta = rep(0, nrow(om)),
  eta_fixed = rep(FALSE, length(start_eta)), modelfunction)
}
\arguments{
\item{cData}{Vector of data points for this individual.}

\item{cTime}{Vector of independent variable (e.g. time points) for this individual.}

\item{theta}{Vector of fixed effects.}

\item{om}{Omega matrix (IIV).}

\item{sig}{igma matrix (RUV)}

\item{start_eta}{Start point for the EBE-search, default = typical values, i.e. vector of 0.}

\item{eta_fixed}{A vector of the same length as start_eta with the number of fixed/constant etas, constant implies that they are fixed to 0, i.e. not the same as OMEGA fixed.}

\item{modelfunction}{The function with the model to use.}
}
\value{
List of individual estimates (EBEs) and the variance-covariace matrix of the estimated EBEs.
}
\description{
Computes individual estimates of parameters using posterior Bayes estimation. Assumes no eta-eps interaction and a homoscedastic error.
}
\examples{
\dontrun{
modelfunc <- function(age,thetas,etas) {
     pred <- calcHAZVector(age,basethetas = thetas[1:6],covthetas=thetas[7:12],etas=etas)
     return(pred)
}

ebe_est <-ind_estimates(cData=as.numeric(obsDf$DV), 
                        cTime=as.numeric(obsDf$AGE), 
                        theta=as.numeric(thetaDf[1,-1]), 
                        om=om, 
                        sig=sig, 
                        start_eta = rep(0,6),
                        modelfunction=modelfunc)
}
}
