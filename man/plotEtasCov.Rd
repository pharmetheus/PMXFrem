% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotEtasCov.R
\name{plotEtasCov}
\alias{plotEtasCov}
\title{Plot FREM ETAs and ETA_prims against a specified covariate}
\usage{
plotEtasCov(
  df,
  covName,
  etaNames = NULL,
  etaTypes = c("FREM", "Prim"),
  etaLabels = NULL,
  typeLabels = c(FREM = "FREM ETA", Prim = "ETA Prim", EBE = "FFEM EBE"),
  xlab = covName,
  ylab = "Value",
  colorNonMissing = "#005B65",
  colorMissing = "#E1A178",
  pointShape = 16,
  pointSize = 1.5,
  pointAlpha = 1,
  smoothMethod = "loess",
  smoothColor = "black",
  smoothLinetype = "solid",
  smoothSize = 1,
  smoothSe = FALSE,
  ...
)
}
\arguments{
\item{df}{A data frame generated by \code{\link{calcEtas}} (run with
\code{appendMissingFlags = TRUE}).}

\item{covName}{A character string specifying the name of the covariate to
plot on the x-axis (e.g., \code{"WT"}).}

\item{etaNames}{A character vector of structural ETAs to include in the plot
(e.g., \code{c("ETA3", "ETA4")}). If \code{NULL} (default), all columns matching
the regex \code{"^ETA[0-9]+$"} will be automatically detected and plotted.}

\item{etaTypes}{A character vector specifying which types of ETAs to plot.
Allowed values are \code{"FREM"}, \code{"Prim"}, and \code{"EBE"}. Defaults to
\code{c("FREM", "Prim")}. The order specified here determines the top-to-bottom
row order in the plotted facets.}

\item{etaLabels}{A character vector of custom labels for the ETA columns. Must be
the same length as \code{etaNames}. If \code{NULL} (default), \code{etaNames} are used.}

\item{typeLabels}{A named character vector specifying custom labels for the ETA
calculation types (the facet rows). Defaults to \code{c("FREM" = "FREM ETA", 
  "Prim" = "ETA Prim", "EBE" = "FFEM EBE")}.}

\item{xlab}{Character string for the x-axis title. Defaults to \code{covName}.}

\item{ylab}{Character string for the y-axis title. Defaults to \code{"Value"}.}

\item{colorNonMissing}{Character string (hex code or color name) for non-missing
points. Defaults to \code{"#005B65"} (dark teal).}

\item{colorMissing}{Character string (hex code or color name) for missing points.
Defaults to \code{"#E1A178"} (peach).}

\item{pointShape}{Integer. The ggplot2 shape code for the points. Defaults to \code{16}.}

\item{pointSize}{Numeric. The size of the points. Defaults to \code{1.5}.}

\item{pointAlpha}{Numeric. The transparency of the points. Defaults to \code{1}.}

\item{smoothMethod}{Character string. The smoothing method to use (e.g., \code{"loess"},
\code{"lm"}). Defaults to \code{"loess"}.}

\item{smoothColor}{Character string for the smoothing line color. Defaults to \code{"black"}.}

\item{smoothLinetype}{Character string for the smoothing line type. Defaults to \code{"solid"}.}

\item{smoothSize}{Numeric. The thickness of the smoothing line. Defaults to \code{1}.}

\item{smoothSe}{Logical. Display confidence interval around smooth? Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed directly to \code{ggplot2::geom_smooth()}
(e.g., \code{span}, \code{formula}).}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a diagnostic scatter plot with a smoothing line, comparing selected
ETA estimates (FREM ETA, ETA_prim, and/or FFEM EBE) against a specified covariate.
The plot is faceted by the ETA type (columns) and the calculation method (rows).
It automatically colors points based on whether the covariate was missing in
the original clinical dataset.
}
\examples{
\dontrun{
library(PMXFrem)
library(ggplot2)

model_dir <- system.file("extdata/SimNeb/", package = "PMXFrem")
data_path <- system.file("extdata/SimNeb/DAT-2-MI-PMX-2-onlyTYPE2-new.csv", package = "PMXFrem")
my_data <- read.csv(data_path)
my_data <- my_data[my_data$BLQ != 1, ] 

ind_params <- calcEtas(
  modName            = "run31",
  modDevDir          = model_dir,
  numNonFREMThetas   = 7,
  numSkipOm          = 2,
  dataFile           = my_data,
  parNames           = c("CL", "V", "MAT"),
  ffemModName        = "run31max0",
  appendMissingFlags = TRUE
)

# Plot with custom labels for publication
p3 <- plotEtasCov(
  df = ind_params, 
  covName = "WT", 
  etaNames = c("ETA3", "ETA4"),
  etaTypes = c("EBE", "FREM"),
  etaLabels = c("Clearance", "Volume"),
  typeLabels = c("EBE" = "FFEM EBE", "FREM" = "FREM EBE")
)
print(p3)
}

}
\seealso{
Other Diagnostics & Plotting: 
\code{\link{calcEtas}()},
\code{\link{calcFremShrinkage}()},
\code{\link{fremParameterTable}()},
\code{\link{getExplainedVar}()},
\code{\link{getForestDFFREM}()},
\code{\link{plotCovDist}()},
\code{\link{plotExplainedVar}()},
\code{\link{traceplot}()}
}
\concept{Diagnostics & Plotting}
\concept{diagnostics}
