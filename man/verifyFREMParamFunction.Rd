% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifyFREMParamFunction.R
\name{verifyFREMParamFunction}
\alias{verifyFREMParamFunction}
\title{Check a generated FREM parameter function}
\usage{
verifyFREMParamFunction(
  x,
  fun = NULL,
  thetas = NULL,
  extFile = NULL,
  dfrows = NULL,
  tol = 1e-06,
  quiet = FALSE
)
}
\arguments{
\item{x}{The list returned by \code{\link[=createFREMParamFunction]{createFREMParamFunction()}}.}

\item{fun}{The function to check. Defaults to \code{eval(parse(text = x$code))}.}

\item{thetas}{A numeric vector of the FREM model's THETA final estimates
(structural \strong{and} covariate means). Defaults to the values read from
\code{extFile}.}

\item{extFile}{Path to the FREM model's \code{.ext}. Required if \code{thetas} is not
supplied. Defaults to the \code{.ext} next to \code{x$fremModel}.}

\item{dfrows}{A data frame of covariate rows to test over. Each row is passed
as \code{dfrow}. Defaults to a single all-reference row (every structural
covariate at \code{x$missVal}).}

\item{tol}{Relative tolerance for a check to pass. Default \code{1e-6}.}

\item{quiet}{If \code{FALSE} (default), prints a per-parameter pass/fail summary.}
}
\value{
A single logical - \code{TRUE} unless a parameter's check actually failed,
so the result can be used directly in an \code{if}. A non-log-normal parameter
(splice not checked, \code{PASS = NA}) does not make the result \code{FALSE}. The
per-parameter detail is attached as \code{attr(x, "checks")}: a data frame with
\code{PARAMETER}, \code{STRUCTURAL} / \code{COVSPLICE} / \code{ETASPLICE} (max relative
difference for each check, \code{NA} when skipped) and \code{PASS} (\code{TRUE} / \code{FALSE} /
\code{NA}). Printing shows the table.
}
\description{
Runs three consistency checks on the function from
\code{\link[=createFREMParamFunction]{createFREMParamFunction()}}, for a set of test covariate rows:
\enumerate{
\item \strong{Structural match.} With \code{covthetas = 0} and \code{etas = 0} a FREM
parameter reduces to its structural typical value. The result is
compared with the SCM-style typical-value function that
\code{\link[PMXForest:createParamFunction]{PMXForest::createParamFunction()}} generates from the same FREM model.
\item \strong{Covariate splice.} Setting \code{covthetas[k]} should scale parameter
\code{k} by \code{exp(covthetas[k])} and nothing else.
\item \strong{Random-effect splice.} Setting \code{etas[numSkipOm + k]} should scale
parameter \code{k} by \code{exp(etas[numSkipOm + k])} and nothing else - this also
checks the \code{numSkipOm} offset is right.
}
Checks 2 and 3 assume the parameter is \strong{log-normal} (\verb{P = C * exp(<linear in ETA>)}). For a parameter whose \verb{$PK} encloses its \code{ETA()} differently -
additive, logit, \code{exp(THETA * ETA)}, ... - the splice is not \code{exp()} scaling,
so \code{COVSPLICE} / \code{ETASPLICE} and \code{PASS} are reported as \code{NA} for that
parameter (the structural check still runs). Anything that actually fails is
reported loudly. Only the FREM \verb{$PK} parameters are checked - \code{secondary}
parameters (AUC, Cmax, ...) are skipped, as they have no structural
counterpart or splice to probe.
}
\examples{
fremModel <- system.file("extdata/SimNeb/run31.mod", package = "PMXFrem")
out <- createFREMParamFunction(fremModel, parameters = c("CL", "V", "MAT"),
                               quiet = TRUE)

if (verifyFREMParamFunction(out, quiet = TRUE)) message("generated function checks out")

v <- verifyFREMParamFunction(out, quiet = TRUE)
attr(v, "checks")          # per-parameter detail

}
\seealso{
\code{\link[=createFREMParamFunction]{createFREMParamFunction()}}.

Other Diagnostics & Plotting: 
\code{\link{calcEtas}()},
\code{\link{calcFremShrinkage}()},
\code{\link{createFREMParamFunction}()},
\code{\link{fremParameterTable}()},
\code{\link{getExplainedVar}()},
\code{\link{getForestDFFREM}()},
\code{\link{plotCovDist}()},
\code{\link{plotEtasCov}()},
\code{\link{plotExplainedVar}()},
\code{\link{traceplot}()}
}
\concept{Diagnostics & Plotting}
\concept{diagnostics}
