% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateFremModel.R
\name{generateFremModel}
\alias{generateFremModel}
\title{Generate a FREM Model File}
\usage{
generateFremModel(
  final_df,
  modelState,
  covList,
  addedList,
  covnames,
  strFREMModel,
  strNewFREMData,
  bWriteMod,
  bWriteFIX,
  noBaseThetas,
  numSkipOm,
  numParCov,
  covEpsNum,
  basenames_th,
  basenames_om,
  dDefaultCovValue,
  strUpdateType
)
}
\arguments{
\item{final_df}{A data.frame containing the finalized FREM dataset, used to dynamically construct the \verb{$INPUT} record.}

\item{modelState}{A list containing the model's current parameter state (including \code{theta}, \code{omegaMatrix}, \code{thetaFix}, \code{numTheta}, \code{numOmega}).}

\item{covList}{A list of lists containing prepared statistical information for new covariates (\code{Name}, \code{Mean}, \code{Var}, \code{Fremtype}).}

\item{addedList}{A character vector of new covariate names added during this update step.}

\item{covnames}{A list containing parsed existing covariate names (\code{covNames}, \code{polyCatCovs}, \code{orgCovNames}).}

\item{strFREMModel}{A character string specifying the file path to the base FREM model (\code{.mod}) file.}

\item{strNewFREMData}{A character string specifying the file path for the newly generated FREM dataset.}

\item{bWriteMod}{Logical. If TRUE, writes the updated model code to disk with a \verb{_new.mod} suffix.}

\item{bWriteFIX}{Logical. If TRUE, appends \code{FIX} to the \verb{$THETA} records for fixed parameters.}

\item{noBaseThetas}{Integer. The number of structural (non-FREM) \verb{$THETA} parameters in the base model.}

\item{numSkipOm}{Integer. The number of independent \verb{$OMEGA} parameters to skip before the main FREM block.}

\item{numParCov}{Integer. The number of structural parameters exhibiting covariate effects.}

\item{covEpsNum}{Integer. The index of the \verb{$SIGMA} parameter (epsilon) used for the FREM covariates.}

\item{basenames_th}{A character vector of names for the structural \verb{$THETA} parameters. If NULL, parsed dynamically from the base model.}

\item{basenames_om}{A character vector of names for the structural \verb{$OMEGA} parameters. If NULL, parsed dynamically from the base model.}

\item{dDefaultCovValue}{Numeric. The default covariance value (typically 1e-05) used to populate structural zeros, ensuring the \verb{$OMEGA} block remains positive-definite.}

\item{strUpdateType}{A character string specifying the update mode (e.g., "DataAndModel", "NoData").}
}
\value{
A character vector containing the lines of the newly generated FREM model file.
}
\description{
Generates the new NONMEM model code based on the final parameter state,
injects the consolidated Full Random Effects Model (FREM) covariate block,
and optionally writes the updated model to disk. It features smart comment
extraction to preserve parameter labels and enforces positive-definite
covariance matrices by replacing structural zeros.
}
\keyword{internal}
