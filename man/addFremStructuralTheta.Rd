% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addFremStructuralTheta.R
\name{addFremStructuralTheta}
\alias{addFremStructuralTheta}
\title{Add a structural \verb{$THETA} (and optionally an IIV) to an established FREM model}
\usage{
addFremStructuralTheta(
  strFREMModel,
  thetaInit,
  parameter = NULL,
  addEta = FALSE,
  muReference = TRUE,
  omegaInit = NULL,
  label = NULL,
  numNonFREMThetas = NULL,
  numSkipOm = NULL,
  extFile = NULL,
  newModel = NULL,
  bWriteMod = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{strFREMModel}{Path to the FREM model file, or a character vector of its
lines.}

\item{thetaInit}{Initial value for the new \verb{$THETA}. Required - a scalar
(e.g. \code{0.1}), a length-3 numeric \code{c(low, init, up)}, or a string written
verbatim after \verb{$THETA} (e.g. \code{"(0, 1.2)"} or \code{"0.5 FIX"}).}

\item{parameter}{Name of the \verb{$PK} variable the new theta belongs to.
Required when \code{addEta = TRUE}; optional otherwise.}

\item{addEta}{Logical. Also add an accompanying IIV (via \code{\link[=addFremIIV]{addFremIIV()}})?
Default \code{FALSE}.}

\item{muReference}{Logical. Only used when \code{addEta = TRUE} - see Details.
Default \code{TRUE}.}

\item{omegaInit}{Initial variance for the new \verb{$OMEGA}. Required when
\code{addEta = TRUE}.}

\item{label}{Comment for the new \verb{$THETA} record. Defaults to
\code{"TV_<parameter>"} or \code{"added structural THETA"}.}

\item{numNonFREMThetas, numSkipOm}{Structural integers. \code{NULL} (default) to
derive them from the model + \code{.ext}; supply to override.}

\item{extFile}{Path to the \code{.ext} for the derivation. Defaults to the model
path with a \code{.ext} extension.}

\item{newModel}{Output path when \code{strFREMModel} is a path and
\code{bWriteMod = TRUE}. Defaults to the input path with \verb{_theta} inserted before
the extension.}

\item{bWriteMod}{Logical. Write the new model? Default \code{TRUE}.}

\item{quiet}{Logical. Suppress progress messages. Default \code{TRUE}.}
}
\value{
Invisibly, a list with \code{model} (character vector), \code{thetaIndex},
\code{etaIndex} (or \code{NA}), \code{numNonFREMThetas}, \code{numSkipOm} (both updated) and
\code{file}.
}
\description{
Inserts one new structural (non-FREM) \verb{$THETA} at index \code{numNonFREMThetas + 1},
immediately before the FREM covariate-mean thetas. Every \code{THETA()} reference
at or after that index in \verb{$PK} / \verb{$ERROR} / \verb{$THETA} is shifted up by one -
in a FREM model that is the \code{MU_k = THETA(numNonFREMThetas + c)} block - so the
model stays consistent. \code{numNonFREMThetas} increases by one.

With \code{addEta = TRUE} the function also adds a matching IIV via
\code{\link[=addFremIIV]{addFremIIV()}} and wires the two together. This is the usual entry point:
adding a structural parameter with a typical value, an MU reference and
between-subject variability in one step.
}
\details{
The existing \code{.ext} / \code{.phi} are \strong{not} migrated - the model must be
re-estimated. Only the control stream is rewritten.

When \code{addEta = TRUE} a new \verb{$PK} definition for \code{parameter} is inserted just
before the FREM \code{MU_k = THETA(...)} block:
\describe{
\item{\code{muReference = TRUE}}{\verb{MU_<k> = THETA(<j>)} then
\verb{<parameter> = EXP(MU_<k> + ETA(<k>))} (log-normal, MU-referenced - the
FREM convention).}
\item{\code{muReference = FALSE}}{\verb{<parameter> = THETA(<j>) * EXP(ETA(<k>))}
(log-normal, no MU line).}
}
where \code{j = numNonFREMThetas + 1} (the new theta) and \code{k = numSkipOm + 1} (the
new eta). With \code{addEta = FALSE} only the \verb{$THETA} record is added; if
\code{parameter} is given its \verb{$PK} line gets a \verb{* THETA(<j>)} factor, otherwise
the caller wires the new theta in by hand.
}
\seealso{
\code{\link[=addFremIIV]{addFremIIV()}}, \code{\link[=updateFREMmodel]{updateFREMmodel()}}

Other FREM model management: 
\code{\link{addFremIIV}()},
\code{\link{createFREMmodel}()},
\code{\link{updateFREMmodel}()}
}
\concept{FREM model management}
\concept{frem_model_management}
